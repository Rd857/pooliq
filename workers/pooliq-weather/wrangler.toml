# Cloudflare Worker config for the PoolIQ weather proxy.
# NOT deployed in Phase 1 — see index.js header comment for deploy steps.
#
# Secrets (set via `wrangler secret put <NAME>`, do NOT put real values here):
#   AMBIENT_API_KEY
#   AMBIENT_APP_KEY

name = "pooliq-weather"
main = "index.js"
compatibility_date = "2024-01-01"
